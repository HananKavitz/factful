pipeline:
  max_passes: 3
  epsilon: 1.0
  delta: 0.5
  score_accept: 85
  revision_mode: patch

corroboration:
  min_sources: 2

gather:
  max_sources: 10

llm:
  base_url: https://openrouter.ai/api/v1
  models:
    style: anthropic/claude-3.5-sonnet
    gather: <provider:model>